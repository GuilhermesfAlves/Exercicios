  program Trabalho_Final!Tatiane Maria Klein Mohr//!Camily Kauane dos Santos
        implicit none !declaraá∆o de vari†veis
        integer ::i,f,j,k,r
        real:: a,s
        character*9:: c
open(unit=2, file='resultados.txt', status='unknown')!abrir programa para armazenar os resultados
print*, 'Relaá∆o de valores da funCAo f(x)=3+2^x durante o intervalo de 1 a 21:'
print*, '/x/     /y/'
do i = 1,21 !loop para fornecer os valores de x e y dentro do intervalo indicado
   print '(i4,a3,f15.2)', i,' ;', func(i) !escrita no terminal
   write (2, '(i4,a3,f15.2)') i,' ;', func(i) !escrita no arquivo
   end do
3 print*, 'em quantos trapezios voce deseja dividir a funcao:'
read*, j
select case (j)
       case (1)
       print*, 'ok, quantidade possivel'
       k=20
       case (2)
       print*, 'ok, quantidade possivel'
       k=10
       case (5)
       print*, 'ok, quantidade possivel'
       k=4
       case (10)
       print*, 'ok, quantidade possivel'
       k=2
       case (20)
       print*, 'ok, quantidade possivel'
       k=1
       case default
       print*, 'quantidade impossivel, tente novamente'
       goto 3
end select
print '(a26)', 'a area de cada trapezio e:'
do i = 1,20,k !loop para o calculo da area dos trapezios
   a = (func(i) + func (i+k))*k/2
   print '(f13.2)', a !escrita no terminal
   write(2, '(f13.2)') a !escrita no arquivo
   s=s+a !loop para a soma total dos trapezios
   end do
f=s-3025610
c='3.025.610'
print*, '(a,/, f13.2, a, /, a, f10.2, //)', 'a area total da soma dos trapezios e:',s, 'a integral Ç c', 'a diferenáa Ç de' , f !escrita terminal
write (2, '(a37,/, f13.2)') ' a area total da soma dos trapezios e:', s  !escrita no arquivo
1 print*, 'voce deseja dividir esse intervalo em outra quantidade:','digite (1) para sim ou (2) para nao'
read*, r
select case(r)
       case(1)
       print*, 'ok!'
       goto 3
       case(2)
       print*, 'ok!'
       case default
       print*, 'n∆o entendi, poderia tentar novamente?'
       goto 1
end select
close (2)!fechar programa que armazena dados
contains
real function func (i) !funá∆o usada para o calculo da integral
     integer::i
     func= 3+2**i
     end function func
end program Trabalho_Final

